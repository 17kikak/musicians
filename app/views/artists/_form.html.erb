
<%= form_with(model: artist, local: true) do |form| %>
  <% if artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

      <ul>
      <% artist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :artist_name %>
  </div>

  <div class="field">
    <%= form.label :genre %>
    <%= form.text_field :genre, id: :artist_genre %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.date_select :age, id: :artist_age %>
  </div>

  <div class="field">
    <%= form.label :net_worth_in_cents %>
    <%= form.number_field :net_worth_in_cents, id: :artist_net_worth_in_cents %>
  </div>

  <div class="field">
    <%= form.label :number_of_albums %>
    <%= form.number_field :number_of_albums, id: :artist_number_of_albums %>
  </div>

  <div class="field">
    <%= form.label :profile_image_url %>
    <%= form.text_field :profile_image_url, id: :artist_profile_image_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
